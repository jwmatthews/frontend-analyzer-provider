[package]
name = "konveyor-core"
version = "0.1.0"
edition = "2021"
description = "Shared Konveyor types: incidents, violations, rulesets, and analysis output format"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
